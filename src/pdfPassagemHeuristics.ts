export type PurchaseData={partida_em:string;valor_passagem:string};
const meses:Record<string,number>={janeiro:1,fevereiro:2,marco:3,'março':3,abril:4,maio:5,junho:6,julho:7,agosto:8,setembro:9,outubro:10,novembro:11,dezembro:12};
const escapeRegExp=(value:string)=>value.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
function labelled(text:string,labels:string[],valuePattern='[^\\n]{2,80}'){const names=labels.map(escapeRegExp).join('|');return text.match(new RegExp(`(?:${names})\\s*[:#-]?\\s*(${valuePattern})`,'i'))?.[1]?.trim()??''}
function parseMoney(value:string){const normalized=value.replace(/\s/g,'').replace(/\./g,'').replace(',','.').replace(/[^\d.]/g,'');const amount=Number(normalized);return Number.isFinite(amount)&&amount>0?amount.toFixed(2):''}
function parseDateTime(value:string){const numeric=value.match(/(\d{1,2})\/(\d{1,2})\/(\d{2,4})(?:[^\d]{1,10}(\d{1,2}):(\d{2}))?/);const textual=value.match(/(\d{1,2})\s+de\s+([a-zç]+)\s+de\s+(\d{4})(?:[^\d]{1,10}(\d{1,2}):(\d{2}))?/i);const source=numeric??textual;if(!source)return'';const day=Number(source[1]);const month=numeric?Number(source[2]):meses[source[2].toLowerCase()];let year=Number(source[3]);if(year<100)year+=2000;const hour=Number(source[4]??0);const minute=Number(source[5]??0);if(!month||day<1||day>31||hour>23||minute>59)return'';return `${year.toString().padStart(4,'0')}-${month.toString().padStart(2,'0')}-${day.toString().padStart(2,'0')}T${hour.toString().padStart(2,'0')}:${minute.toString().padStart(2,'0')}`}
export function extractTicketDataFromText(rawText:string):Partial<PurchaseData>{const text=rawText.replace(/\r/g,'\n').replace(/[ \t]+/g,' ');const result:Partial<PurchaseData>={};const partida=parseDateTime(labelled(text,['Partida','Embarque','Saída','Saida'],'[^\\n]{5,80}'));const valor=parseMoney(labelled(text,['Valor total','Total','Tarifa','Preço','Preco'],'(?:R\\$\\s*)?[\\d.]+,\\d{2}'));if(partida)result.partida_em=partida;if(valor)result.valor_passagem=valor;return result}
