import {useEffect,useState} from 'react';import {Navigate,Route,Routes} from 'react-router-dom';import type {Session} from '@supabase/supabase-js';import {configured,supabase} from './supabase';import {Header,Sidebar,Spinner} from './components';import {Dashboard,Detalhe,Login,NovaSolicitacao,Responsaveis,Solicitacoes} from './pages';
export type Access={isRO:boolean;isAdmin:boolean;canViewAll:boolean;canOperateRO:boolean;canManageRO:boolean;role:string|null};
// eslint-disable-next-line react-hooks/exhaustive-deps
export function App(){const empty:Access={isRO:false,isAdmin:false,canViewAll:false,canOperateRO:false,canManageRO:false,role:null};const [session,setSession]=useState<Session|null>(null);const [loading,setLoading]=useState(true);const [side,setSide]=useState(false);const [access,setAccess]=useState<Access>(empty);useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setLoading(false)});const {data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]);useEffect(()=>{if(!session){setAccess(empty);return}Promise.all([supabase.from('user_roles').select('role').eq('user_id',session.user.id),supabase.from('ro_responsaveis').select('id').eq('user_id',session.user.id).eq('ativo',true).maybeSingle()]).then(([roles,ro])=>{const names=(roles.data||[]).map((r)=>String(r.role));const role=names[0]||null;const isAdmin=names.some(r=>['gerente','diretor'].includes(r));const isRO=Boolean(ro.data);setAccess({role,isRO,isAdmin,canViewAll:isAdmin||isRO,canOperateRO:isRO,canManageRO:isAdmin})})},[session]);if(loading)return <div className="full"><Spinner/></div>;if(!session)return <Login/>;return <div className="shell"><Sidebar open={side} onClose={()=>setSide(false)} onLogout={()=>supabase.auth.signOut()} admin={access.canManageRO}/><section className="content"><Header onMenu={()=>setSide(true)}/><Routes><Route path="/" element={<Dashboard access={access}/>}/><Route path="/solicitacoes" element={<Solicitacoes access={access} userId={session.user.id}/>}/><Route path="/nova" element={<NovaSolicitacao userId={session.user.id}/>}/><Route path="/solicitacoes/:id" element={<Detalhe access={access}/>}/><Route path="/responsaveis" element={access.canManageRO?<Responsaveis/>:<Navigate to="/"/>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></section></div>}
export function EnvWarning(){return !configured?<div className="alert">Configure o arquivo <code>.env.local</code> usando o modelo <code>.env.example</code>.</div>:null}
