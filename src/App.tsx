import {useEffect,useState} from 'react';import {Navigate,Route,Routes} from 'react-router-dom';import type {Session} from '@supabase/supabase-js';import {configured,supabase} from './supabase';import {Header,Sidebar,Spinner} from './components';import {Dashboard,Detalhe,Login,NovaSolicitacao,Responsaveis,Solicitacoes} from './pages';
export type Access={isRO:boolean;isAdmin:boolean;role:string|null};
export function App(){const [session,setSession]=useState<Session|null>(null);const [loading,setLoading]=useState(true);const [side,setSide]=useState(false);const [access,setAccess]=useState<Access>({isRO:false,isAdmin:false,role:null});useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setLoading(false)});const {data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]);useEffect(()=>{if(!session){setAccess({isRO:false,isAdmin:false,role:null});return}Promise.all([supabase.from('user_roles').select('role').eq('user_id',session.user.id),supabase.from('ro_responsaveis').select('id').eq('user_id',session.user.id).eq('ativo',true).maybeSingle()]).then(([roles,ro])=>{const names=(roles.data||[]).map((r)=>String(r.role));const role=names[0]||null;setAccess({role,isRO:Boolean(ro.data)||names.some(r=>['gerente','diretor'].includes(r)),isAdmin:names.some(r=>['gerente','diretor'].includes(r))})})},[session]);if(loading)return <div className="full"><Spinner/></div>;if(!session)return <Login/>;return <div className="shell"><Sidebar open={side} onClose={()=>setSide(false)} onLogout={()=>supabase.auth.signOut()} admin={access.isAdmin}/><section className="content"><Header onMenu={()=>setSide(true)}/><Routes><Route path="/" element={<Dashboard access={access}/>}/><Route path="/solicitacoes" element={<Solicitacoes access={access} userId={session.user.id}/>}/><Route path="/nova" element={<NovaSolicitacao userId={session.user.id}/>}/><Route path="/solicitacoes/:id" element={<Detalhe access={access}/>}/><Route path="/responsaveis" element={access.isAdmin?<Responsaveis/>:<Navigate to="/"/>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></section></div>}
export function EnvWarning(){return !configured?<div className="alert">Configure o arquivo <code>.env.local</code> usando o modelo <code>.env.example</code>.</div>:null}
